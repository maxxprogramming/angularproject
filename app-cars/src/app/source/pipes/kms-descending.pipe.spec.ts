import { KmsDescendingPipe } from './kms-descending.pipe';

describe('KmsDescendingPipe', () => {
  it('create an instance', () => {
    const pipe = new KmsDescendingPipe();
    expect(pipe).toBeTruthy();
  });
});
