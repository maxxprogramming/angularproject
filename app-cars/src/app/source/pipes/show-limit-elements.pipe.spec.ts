import { ShowLimitElementsPipe } from './show-limit-elements.pipe';

describe('ShowLimitElementsPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowLimitElementsPipe();
    expect(pipe).toBeTruthy();
  });
});
